filebeat:
  prospectors:
  - input_type: log
    paths:
      - /var/log/node/current
    json.message_key: event
    json.keys_under_root: true
    fields:
      service: ${LOG_SERVICE_NAME:nodeapp}
      host_ip: ${HOST_IP:0.0.0.0}
